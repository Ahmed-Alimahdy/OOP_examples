class relational
{
	int x;
	int y;
public:
	relational(int x = 0, int y = 0) :x(x), y(y) {}
	bool operator!=(relational b)
	{
		return(x != b.x && y != b.y);
	}
	void show()
	{
		cout << x << " " << y << '\n';
	}
};